[project]
name = "rokenbok-webserver"
version = "0.3.0"
readme = "README.md"
requires-python = ">=3.14.3, <3.15"
dependencies = [
    "Flask",
    "Flask-Session",
    "Flask-SocketIO",
    "pyinstaller",
    "pyserial",
    "PyYAML",
    "requests"
]
